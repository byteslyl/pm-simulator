# AGENTS.md — CareerCraft 项目决策模拟器 · 自动迭代工作协议

> 本文件是给 AI 编码代理（Codex）的常驻工作协议。每次会话自动读取并遵守。
> 本文件与 `docs/requirements.md` 是唯一被允许长期维护并无限迭代的两份"活文档"。

---

## 一、项目是什么（一句话）

CareerCraft 是训练"**信息不完全条件下的结构化决策能力**"的多场景决策仿真平台。学生扮演项目负责人，通过访谈 4 个立场冲突的 AI 干系人获取分散、分层、稀缺、含噪的信息，再外化出一个结构化决策模型，提交决策、承担后果、获得证据化复盘。

**主线必须死守：训练的是"信息稀缺下的结构化决策闭环"，"多 Agent / LLM"只是实现手段，不是产品本体。**

## 二、不可违背的硬约束（Non-Negotiable）

1. **Mock 写死，先不接 LLM。** 当前迭代所有角色对话、意图分类、评分全部走规则引擎 + 预置数据（关键词意图分类 + 场景真值表 + 预置角色台词）。禁止新增任何对 LLM 的真实网络调用。可为"未来接 LLM"预留接口，但不实现。
   - v0.2 起后端强制 `ruleEngineOnly=true`、`llmEnabled=false`，即使环境变量存在也不得启用真实 LLM 调用。
2. **先完善场景一，再迁移多场景。** 只做「上线前夜 · 星盘结算」这一个场景。场景二（跨部门资源争夺）、场景三（危机应急）**本轮不做**，仅在 requirement 中保留路线图。
3. **披露决定权在规则引擎，不在对话层。** 真值表 `engine/scenario_data.json` 是唯一数据源。任何角色回复都不得创造真值表以外的数字/事实，也不得越权说别的角色的私有信息。
4. **不破坏评分公平性。** 状态变化与评分由规则引擎计算，对话生成只负责"用角色口吻怎么说"。
5. **前端必须"高大上"**，但功能优先于装饰：每个交互元素必须是真实组件，禁止静态假按钮。
6. **API 先做场景合法性校验。** 角色 ID、决策字段、证据引用等外部入参要在 API/引擎边界返回清晰 400，不能把非法别名传进规则引擎造成内部异常。
7. **前端展示可重构，但不能丢训练机制。** 入口页、工作区、复盘页可以按顶级产品案例推翻重做；必须保留干系人访谈、稀缺预算、事实面板、六要素工作台、D1/D2、复盘报告这些核心元素。
8. **客户演示层不得暴露内部实现。** 前端可见文案不得出现 Mock、未接 LLM、规则引擎、关键词兜底、原始真值表数量等开发口径；统一用产品化语言表达为智能协同训练、证据图谱、行动窗口、关键风险、决策边界、智能评估。

## 三、自动迭代协议（Infinite Loop）

每个迭代轮次严格按此顺序执行，且**每轮结束必须用 TodoWrite 记录进度**：

1. **读** `docs/requirements.md`（需求真源）→ 2. **读** `AGENTS.md`（本协议）→ 3. **盘点** `engine/scenario_data.json` 与 `frontend/index.html` 现状 → 4. **设计** 本轮要交付的能力 → 5. **实现**（遵循第六节标杆借鉴）→ 6. **自测**（`npm test` + 手动 API 冒烟）→ 7. **更新** `docs/requirements.md`（勾选已完成、记录决策、追加下一优先级）→ 8. **回写** 本协议中任何新发现的对齐规则。

**迭代永不停止**：每轮结束都以"requirements 中下一个最高优先级 P0/P1 项"作为下一轮目标，直到用户叫停。

## 四、唯一允许维护/新增的文档

- `docs/requirements.md` — 需求文档（唯一需求真源，无限迭代）
- `AGENTS.md` — 本协议（对齐规则，发现偏差时回写）
- `docs/demo-walkthrough.md` — 用户明确要求维护的验收与汇报演示手册，只记录可复现体验路径、讲解口径和演示断点，不替代需求真源

**禁止**新增其它任何 `.md`/README/说明文档，除非用户明确要求。所有设计结论写入上述两份文档，不要散落在别处。

## 五、模块 → 必须借鉴的市场已验证标杆（业务逻辑，非技术栈）

实现任何模块前，先研究对应标杆的**业务逻辑**，再决定交互，禁止凭空设计：

| 模块 | 借鉴对象 | 借鉴的业务逻辑 |
|------|---------|---------------|
| 干系人访谈/对话 | ChatGPT/Claude、Her Story、Return of the Obra Dinn、逆转裁判、极乐迪斯科 | 信息收集型"对话产出线索"闭环；线索→推理→结论的逐步拼装；证据在对话中自然浮现而非列表堆砌 |
| 决策工作台/证据墙 | Miro/Obsidian、侦探游戏线索板、Notion 数据库视图 | 把"零散事实"拖拽聚合为"结构化决策模型"；证据挂接可回溯 |
| 结构化决策表单 | Harvard/Cesim/Capsim 商业模拟 | 决策前必须显式填写（目标/约束/方案/风险/证据）而非只选按钮 |
| 五维评分与复盘报告 | Duolingo 学习报告、电竞/健身赛后数据面板 | 用雷达图+分维度归因，让"哪里差"一眼可见；反事实对比 |
| 脚手架递减训练 | Duolingo/Khan Academy（mastery learning） | 第一遍给引导填空→第二遍撤引导→平行场景验证迁移，而非原场景重试 |
| 稀缺/时间资源 | This War of Mine、Frostpunk | "机会成本"可视化：每次访谈消耗时间预算，必须取舍，无法问遍所有人 |

## 六、技术现状速查（避免重复造轮子）

- 后端：Express（`backend/server.js`），REST API；前端：单文件 `frontend/index.html`（原生 JS+CSS）。
- 引擎：`engine/index.js` 串起 `constraint_engine`（7 硬约束）、`state_engine`（S1/S2/S3）、`relationship_engine`（五档信任+正式询问）、`scoring_engine`（五维）、`result_engine`（RR-1~6）。
- 场景真值表：`engine/scenario_data.json`（17 事实 / 4 角色 / 7 约束 / 6 结果规则），`timeline.real_time_budget_minutes: 40` 已由引擎消费并在前端展示。
- 运行：`npm install && npm start` → http://localhost:3000（前端使用同源 `/api`，便于本地和部署环境共用）。
- 测试：`npm test`（backend/tests/api.test.js）。

## 七、当前 P0 待办（来自评审问询，需优先闭环）

- **P1 访谈体验优化**：强化线索浮现、机会成本反馈、跨角色引用反馈。
- **P1 复盘报告二次打磨**：继续强化反事实对比、错因定位和迁移建议。
- **P1 视觉验证自动化**：入口页、三栏工作区、D2 工作台、复盘页需要浏览器截图与移动端断言。
- **路线图**：场景二/三仅写入 requirements，不实现。
